reviewed/*.json
# CI-built signing output — scratch, never committed. Pre-signed
# artifacts for skip_signing deploys are committed under signed/ instead.
out/
# Default SERVE_DIR — the tree ci-assemble.sh assembles for rsync.
serve/
previous.json
